name: Build and Upload BCH Stealth Plugin

on:
  push:
    tags:
      - 'plugin-v*'

jobs:
  build-and-upload:
    name: Build stealth.zip and upload to release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build stealth-ec-plugin.zip
        run: |
          cd stealth
          zip -r ../stealth-ec-plugin.zip . -x "__pycache__/*" "*.pyc" ".DS_Store"
          cd ..
          echo "Built stealth-ec-plugin.zip:"
          unzip -l stealth-ec-plugin.zip

      - name: Generate sha256 checksum
        run: |
          sha256sum stealth-ec-plugin.zip > stealth-ec-plugin.zip.sha256
          echo "SHA256: $(cat stealth-ec-plugin.zip.sha256)"

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            stealth-ec-plugin.zip
            stealth-ec-plugin.zip.sha256
